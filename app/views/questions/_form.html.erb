<%= form_with model: @question, local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p><br>

  <p><%= form.submit %></p>
<% end %>

<%= link_to 'Back', test_path(@question.test_id) %>
<h1><%= question_header(@test) %></h1>

<%= form_with model: [@test, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>
  
  <%= render partial: 'question_form', locals: {form: form} %>
<% end %>

<%= link_to 'Back', test_path(@test) %>
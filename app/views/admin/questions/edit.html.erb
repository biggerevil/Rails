<h1><%= question_header(@test) %></h1>

<%= form_with model: [:admin, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <%= render partial: 'form_fields', locals: {form: form} %>
<% end %>

<%= link_to 'Back', admin_test_path(@question.test_id) %>